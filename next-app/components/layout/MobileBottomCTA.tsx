"use client";

// MobileBottomCTA — disabled per user request (Jun 2026).
// The fixed bottom bar with "Launch App" + "Submit" has been removed.
// File kept to avoid import errors in layout.tsx.

export function MobileBottomCTA() {
  return null;
}
